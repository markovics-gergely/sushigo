import { RespMatGridTileDirective } from './resp-mat-grid-tile.directive';

describe('RespMatGridTileDirective', () => {
  it('should create an instance', () => {
    const directive = new RespMatGridTileDirective();
    expect(directive).toBeTruthy();
  });
});
